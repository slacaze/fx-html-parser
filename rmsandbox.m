function rmsandbox()
    if ~isempty( ver( 'mcam' ) )
        fx.mcam.command.rmsandbox();
    else
        thisPath = fileparts( mfilename( 'fullpath' ) );
        rmpath( fullfile(...
            thisPath,...
            'code',...
            'html' ) );
        rmpath( fullfile(...
            thisPath,...
            'test' ) );
    end
end